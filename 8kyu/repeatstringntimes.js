//PREP
//Parameters will be a number to repeat the string that many times

function repeatStr(n, s) {
    //Example 6, 'i' will return iiiiii
    //Psuedo Code
    //use the repeat method on s, n times
    return s.repeat(n);

    //Return the return will be the string repeated n times
}
// PREP
// Parameters. I will be passed a number in the form of a string. 
const stringToNumber = function (str) {
    // Example. If given a string '1' what will be returned is 1 the number
    // PsuedoCode 
    //   Take string and use either Number or parseInt and then return
    return parseInt(str);

    //Return. What will be returned is the string turned into a number
}